---
description: "PUA 默认模式。默认从 L1 开始，并在重复失败、缺少验证、原地打转或过早放弃时升级到 L2 / L3。触发命令：'/pua:auto'。"
---

加载 `pua` skill。

## 执行规则

1. 如果有参数，将其视为任务描述
2. 用 Skill tool 加载 `pua` skill
3. 默认从 L1 开始；如果出现连续失败、没有验证就想宣称完成、未验证就甩锅、或明显原地打转，则升级到 L2 / L3
